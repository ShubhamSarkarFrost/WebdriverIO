// in macintosh we can create a new directory using mkdir command and then get into the directory using cd command example
// mkdir wdio-course && cd wdio-course
// to initialize a npm project we are going to use npm init -y
// next step is to install webdriver IO CLI npm install @wdio/cli
// next run npx wdio config
